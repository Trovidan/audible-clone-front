import React from 'react';
import './styles.css';

export default function PurchaseDetails() {
    return (
        <div className="user-profile-error">
            <a href="http://localhost:3000">
                Let's listen to a book to review
            </a>
        </div>
    );
}